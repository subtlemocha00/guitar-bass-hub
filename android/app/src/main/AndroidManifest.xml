<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Tuner microphone (Phase 5). Capacitor's WebView bridge (BridgeWebChromeClient
         .onPermissionRequest) turns the tuner's getUserMedia into a runtime permission
         request. For an AUDIO_CAPTURE request it asks for BOTH permissions below and
         grants the WebView only if EVERY one is granted. RECORD_AUDIO is the runtime
         (dangerous) prompt; MODIFY_AUDIO_SETTINGS is a normal permission auto-granted
         at install *only if declared here*. Omitting MODIFY_AUDIO_SETTINGS makes its
         runtime check return denied with no dialog, so Capacitor denies the whole
         request and getUserMedia rejects with NotAllowedError even though RECORD_AUDIO
         shows as allowed. Both are required. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
</manifest>
